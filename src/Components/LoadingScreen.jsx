import { motion as m } from 'framer-motion';
import art4 from '../assets/art4.png'

const LoadingScreen = () => {
  return (
    <div 
      // initial={{opacity: 0}} 
      // animate={{opacity: 1}}
      // transition={{duration:0.75, ease: 'easeOut'}}
      className='h-screen bg-[#849E8F]'>
    </div>
  )
}

export default LoadingScreen
